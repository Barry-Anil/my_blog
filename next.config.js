/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          hostname: "proficient-narwhal-245.convex.cloud",
        },
        
      ],
    },
  };
  
  module.exports = nextConfig;